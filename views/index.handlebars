
<section id="titleText" name="title">
    {{{ homepage.titleText }}}
</section>
<section id="pageContent" name="content">
    {{{ homepage.pageContent }}}
</section>


{{#if other}}
<div class="half-box">
<a href="{{other.link}}">Switch to {{other.name}} mode</a>
</div>
{{/if}}
